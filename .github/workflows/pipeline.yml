name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: pipeline-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  PROD_PROJECT: lupin-prod
  DEV_PROJECT: lupin-demo

jobs:
  ci:
    name: CI — Lint, Test, Build
    runs-on: self-hosted
    defaults:
      run:
        shell: powershell

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ── Backend lint ──────────────────────────────────────────
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Install backend dependencies
        working-directory: backend
        run: uv sync

      - name: Backend lint (ruff check)
        working-directory: backend
        run: uv run ruff check app/ tests/

      - name: Backend lint (ruff format)
        working-directory: backend
        run: uv run ruff format --check app/ tests/

      # ── Backend test ──────────────────────────────────────────
      - name: Start test Postgres (port 5433)
        run: |
          docker rm -f ci-postgres 2>$null; $LASTEXITCODE = 0
          docker run -d --name ci-postgres `
            -e POSTGRES_USER=user `
            -e POSTGRES_PASSWORD=password `
            -e POSTGRES_DB=shouldcost_test `
            -p 5433:5432 `
            postgres:16-alpine
          for ($i = 1; $i -le 30; $i++) {
            docker exec ci-postgres pg_isready -U user 2>$null
            if ($LASTEXITCODE -eq 0) { break }
            Start-Sleep -Seconds 1
          }

      - name: Backend tests
        working-directory: backend
        env:
          DATABASE_URL: postgresql+asyncpg://user:password@localhost:5433/shouldcost_test
          AZURE_OPENAI_ENDPOINT: ${{ secrets.AZURE_OPENAI_ENDPOINT }}
          AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
          AZURE_OPENAI_DEPLOYMENT_NAME: ${{ secrets.AZURE_OPENAI_DEPLOYMENT_NAME }}
          AZURE_OPENAI_API_VERSION: ${{ secrets.AZURE_OPENAI_API_VERSION }}
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          UPLOAD_DIR: C:\Temp\uploads
          CORS_ORIGINS: '["http://localhost:3000"]'
        run: uv run pytest tests/ -v --cov=app --cov-report=term-missing

      - name: Stop test Postgres
        if: always()
        run: docker rm -f ci-postgres 2>$null; exit 0

      # ── Frontend lint ─────────────────────────────────────────
      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci --legacy-peer-deps

      - name: Frontend lint
        working-directory: frontend
        run: npm run lint

  deploy:
    name: Deploy to Self-Hosted Machine
    runs-on: self-hosted
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    defaults:
      run:
        shell: powershell

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Write .env file
        run: |
          $content = @'
          ${{ secrets.ENV_FILE }}
          '@
          [System.IO.File]::WriteAllText(".env", $content.Trim())

      # ── Build & push images ───────────────────────────────────
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}/backend:latest
            ${{ env.REGISTRY }}/${{ github.repository }}/backend:${{ github.sha }}

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}/frontend:latest
            ${{ env.REGISTRY }}/${{ github.repository }}/frontend:${{ github.sha }}
          build-args: |
            VITE_API_URL=
            VITE_APP_NAME=AI Should Cost Engine

      # ── Deploy ────────────────────────────────────────────────
      - name: Scale down existing stacks (free ports)
        run: |
          docker compose -p ${{ env.DEV_PROJECT }} down --remove-orphans 2>$null
          docker compose -p ${{ env.PROD_PROJECT }} -f docker-compose.prod.yml down --remove-orphans 2>$null
          exit 0

      - name: Deploy with Docker Compose
        env:
          BACKEND_IMAGE: ${{ env.REGISTRY }}/${{ github.repository }}/backend:latest
          FRONTEND_IMAGE: ${{ env.REGISTRY }}/${{ github.repository }}/frontend:latest
        run: docker compose -p ${{ env.PROD_PROJECT }} -f docker-compose.prod.yml up -d --remove-orphans

      - name: Verify deployment
        run: |
          Start-Sleep -Seconds 10
          docker compose -p ${{ env.PROD_PROJECT }} -f docker-compose.prod.yml ps
          curl -f http://localhost:8000/health

      - name: Deployment summary
        run: |
          Add-Content $env:GITHUB_STEP_SUMMARY "## Deployment Complete"
          Add-Content $env:GITHUB_STEP_SUMMARY "- **Frontend:** http://localhost:3000"
          Add-Content $env:GITHUB_STEP_SUMMARY "- **Backend:** http://localhost:8000"
          Add-Content $env:GITHUB_STEP_SUMMARY "- **Commit:** ``${{ github.sha }}``"
